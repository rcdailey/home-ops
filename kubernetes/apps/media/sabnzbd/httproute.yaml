---
# yaml-language-server: $schema=https://kubernetesjsonschema.dev/gatewayapi/httproute-v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sabnzbd
spec:
  hostnames:
  - nzb.${SECRET_DOMAIN}
  parentRefs:
  - name: external
    namespace: network
    sectionName: https
  rules:
  - backendRefs:
    - name: sabnzbd
      port: 8080
