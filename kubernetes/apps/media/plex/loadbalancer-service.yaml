---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/v1/service.json
apiVersion: v1
kind: Service
metadata:
  name: plex-direct
  labels:
    app.kubernetes.io/component: proxy
  annotations:
    lbipam.cilium.io/ips: 192.168.50.100
spec:
  type: LoadBalancer
  ports:
  - port: 32400
    targetPort: 32400
  selector:
    app.kubernetes.io/name: plex
