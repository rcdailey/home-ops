---
# yaml-language-server: $schema=https://www.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ./crd.yaml
- ./httproute.yaml
- ./externalsecret.yaml
configMapGenerator:
- name: cloudflare-dns-shared-values
  files:
  - values.yaml=shared-values.yaml
generatorOptions:
  disableNameSuffixHash: true
