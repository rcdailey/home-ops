---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dns-private
resources:
- ./dnsendpoint.yaml
- ./helmrelease.yaml
- ./pvc.yaml
- ./secret.sops.yaml
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
- name: technitium-advanced-forwarding
  files:
  - dnsApp.config=advancedForwarding.json
configurations:
- kustomizeconfig.yaml
