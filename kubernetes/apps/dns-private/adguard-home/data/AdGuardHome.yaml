---
# Explicitly disable authentication (overrides existing runtime config)
# Access restricted to internal network via internal gateway
users: []

dns:
  trusted_proxies:
  - 127.0.0.0/8
  - ::1/128
  - 192.168.1.1
