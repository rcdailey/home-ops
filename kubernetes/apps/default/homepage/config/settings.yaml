---
# Settings configuration for Homepage
# This file contains global settings that control the appearance and behavior
# of the homepage dashboard.
#
# Key settings:
# - title: Browser tab title and main heading
# - favicon: Icon displayed in browser tab and bookmarks
# - theme: Color scheme (light, dark, or color name)
# - layout: Per-group layout configuration (style, columns, heights)
# - quicklaunch: Search functionality settings
#
# Use ${SECRET_DOMAIN} for domain substitution via Flux.
title: Nami
favicon: https://raw.githubusercontent.com/rcdailey/home-ops/main/kubernetes/apps/default/homepage/icons/nami.png

# Theme configuration
theme: dark
color: slate
iconStyle: theme

# Layout configuration with tabs
layout:
  # Main tab - frequently used services
  Dashboard:
    tab: Main
    style: column
    columns: 4
  # Media tab - arr stack and media tools
  Media Automation:
    tab: Media
    style: column
    columns: 4
  Media Tools:
    tab: Media
    style: column
    columns: 4
  # Infra tab - infrastructure, monitoring, external
  Infrastructure:
    tab: Infra
    style: row
    columns: 4
  Monitoring:
    tab: Infra
    style: row
    columns: 4
    initiallyCollapsed: true
  External Services:
    tab: Infra
    style: row
    columns: 6
    initiallyCollapsed: true

# Global settings for compactness
useEqualHeights: true
maxGroupColumns: 6
headerStyle: clean
statusStyle: dot
target: _self

# Quick launch settings
quicklaunch:
  searchDescriptions: true
  hideInternetSearch: false
  showSearchSuggestions: true
  hideVisitURL: false
  provider: google

# Hide version info
hideVersion: true
