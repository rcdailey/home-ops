---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: searxng
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  values:
    controllers:
      searxng:
        annotations:
          reloader.stakater.com/auto: "true"
        strategy: RollingUpdate
        initContainers:
          init-config:
            image:
              repository: alpine
              tag: latest # don't want to deal with renovate upgrades
            command: ["/bin/sh", "-c"]
            args:
            - |
              sed -e "s|%%SECRET_KEY%%|$SECRET_KEY|g" \
                  -e "s|%%BRAVE_API_KEY%%|$BRAVE_API_KEY|g" \
                  -e "s|%%WOLFRAM_API_KEY%%|$WOLFRAM_API_KEY|g" \
                  -e "s|%%REDDIT_CLIENT_ID%%|$REDDIT_CLIENT_ID|g" \
                  /config-template/settings.yml > /config/settings.yml
              cp /config-template/limiter.toml /config/limiter.toml
            envFrom:
            - secretRef:
                name: searxng-secret
        containers:
          app:
            image:
              repository: ghcr.io/searxng/searxng
              tag: 2026.4.4-69f04d59d
            env:
              TZ: America/Chicago
              SEARXNG_BASE_URL: https://search.${SECRET_DOMAIN}
              SEARXNG_PORT: &port 8080
            probes:
              # Exec probe with X-Real-IP header prevents "X-Forwarded-For nor X-Real-IP header is set!" warning
              liveness:
                enabled: true
                custom: true
                spec:
                  exec:
                    command: ["wget", "-q", "--spider", "--header", "X-Real-IP: 127.0.0.1", "http://localhost:8080/healthz"]
                  timeoutSeconds: 5
            resources:
              requests:
                cpu: 10m
                memory: 128Mi
              limits:
                memory: 512Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop: [ALL]

        pod:
          securityContext:
            runAsUser: 977
            runAsGroup: 977
            runAsNonRoot: true
            fsGroup: 977
            fsGroupChangePolicy: OnRootMismatch

    service:
      app:
        controller: searxng
        ports:
          http:
            port: *port

    route:
      app:
        hostnames:
        - search.${SECRET_DOMAIN}
        parentRefs:
        - name: internal
          namespace: network
          sectionName: https
        rules:
        - backendRefs:
          - name: searxng
            port: *port

    persistence:
      config-template:
        type: configMap
        name: searxng-config
        advancedMounts:
          searxng:
            init-config:
            - path: /config-template
              readOnly: true
      # Custom Reddit engine with OAuth2 support overlaid onto built-in engine
      reddit-engine:
        type: configMap
        name: searxng-config
        advancedMounts:
          searxng:
            app:
            - path: /usr/local/searxng/searx/engines/reddit.py
              subPath: reddit.py
              readOnly: true
      config:
        type: emptyDir
        advancedMounts:
          searxng:
            init-config:
            - path: /config
            app:
            - path: /etc/searxng
              readOnly: true
      tmp:
        type: emptyDir
        advancedMounts:
          searxng:
            app:
            - path: /tmp
