---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/monitoring.coreos.com/servicemonitor_v1.json
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: opencloud
spec:
  selector:
    matchLabels:
      observability.home-ops/metrics: opencloud
  endpoints:
  - port: metrics
