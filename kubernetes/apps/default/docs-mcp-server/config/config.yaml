---
app:
  telemetryEnabled: false
  embeddingModel: accounts/fireworks/models/qwen3-embedding-8b

server:
  host: 0.0.0.0
  publicOrigin: https://aidocs.${SECRET_DOMAIN}

scraper:
  maxConcurrency: 3
  security:
    fileAccess:
      mode: disabled
