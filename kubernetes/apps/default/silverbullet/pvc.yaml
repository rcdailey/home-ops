# yaml-language-server: $schema=https://www.schemastore.org/kubernetes.json
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: silverbullet-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-block
