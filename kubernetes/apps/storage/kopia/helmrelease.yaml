---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kopia
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  interval: 1h
  values:
    controllers:
      kopia:
        annotations:
          reloader.stakater.com/auto: "true"
        strategy: Recreate
        containers:
          app:
            image:
              repository: ghcr.io/home-operations/kopia
              tag: 0.22.0@sha256:aa6a2f5987b9c991a349c34e0c7ef80f00366d84490d7637355b10d38fcbc034
            env:
              KOPIA_WEB_ENABLED: "true"
              KOPIA_WEB_PORT: &port 80
              TZ: America/New_York
            envFrom:
            - secretRef:
                name: kopia-secret
            args:
            - --without-password
            - --ui-preferences-file=/config/ui-preferences.json
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: *port
                  # Large repository (31k+ index blobs) over NFS requires extended startup time
                  initialDelaySeconds: 30
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop: [ALL]
            resources:
              requests:
                cpu: 10m
                memory: 512Mi
              limits:
                memory: 512Mi

    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch

    service:
      app:
        controller: kopia
        ports:
          http:
            port: *port

    route:
      app:
        parentRefs:
        - name: internal
          namespace: network
          sectionName: https
        hostnames:
        - kopia.${SECRET_DOMAIN}

    persistence:
      config-file:
        type: configMap
        name: kopia-config
        globalMounts:
        - path: /config/repository.config
          subPath: repository.config
        - path: /config/ui-preferences.json
          subPath: ui-preferences.json
      repository:
        type: nfs
        server: 192.168.1.58
        path: /mnt/user/volsync
        globalMounts:
        - path: /repository
      tmpfs:
        type: emptyDir
        advancedMounts:
          kopia:
            app:
            - path: /config/cache
              subPath: cache
            - path: /config/logs
              subPath: logs
            - path: /tmp
              subPath: tmp
