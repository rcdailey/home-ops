---
# yaml-language-server: $schema=https://www.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: observability
components:
- ../../components/common
- ../../components/drift-detection
resources:
# Observability stack re-enabled with Ceph write amplification mitigation:
# - Storage: hostPath on worker nodes (not Ceph) for metrics/logs
# - Metrics: Opt-in collection (infrastructure + media/default/home namespaces only)
# - Logs: Disabled centralized Vector; using sidecar pattern (Plex example)
- ./victoria-metrics-k8s-stack/ks.yaml
- ./victoria-logs-single/ks.yaml
- ./grafana/ks.yaml
- ./gatus/ks.yaml
- ./silence-operator/ks.yaml
- ./vmrules
- ./blackbox-exporter/ks.yaml
