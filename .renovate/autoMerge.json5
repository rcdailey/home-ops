{
  $schema: "https://docs.renovatebot.com/renovate-schema.json",
  packageRules: [
    {
      description: "Auto-merge GitHub Actions",
      matchManagers: ["github-actions"],
      automerge: true,
      automergeType: "branch",
      matchUpdateTypes: ["minor", "patch", "digest"],
      minimumReleaseAge: "3 days",
      ignoreTests: true
    },
    {
      // IMPORTANT: Specific packageRules must come BEFORE broader rules. Renovate evaluates
      // packageRules sequentially and the first matching rule wins. If this Python-specific
      // rule came after the general Mise rule below, Python would match the broader rule
      // first and incorrectly auto-merge minor versions.
      //
      // Python minor version updates frequently introduce breaking changes to stdlib APIs
      // Example: Python 3.14's BooleanOptionalAction no longer accepts 'type' parameter,
      // breaking flux-local and other tools. Only auto-merge patch versions (security fixes).
      description: "Auto-merge Mise Python (patch only)",
      matchManagers: ["mise"],
      matchPackageNames: ["python"],
      automerge: true,
      automergeType: "branch",
      matchUpdateTypes: ["patch"],
      ignoreTests: true
    },
    {
      description: "Auto-merge Mise Tools (except Python)",
      matchManagers: ["mise"],
      automerge: true,
      automergeType: "branch",
      matchUpdateTypes: ["minor", "patch"],
      ignoreTests: true
    },
    {
      description: "Auto-merge container patch and digest updates",
      matchDatasources: ["docker"],
      automerge: true,
      automergeType: "pr",
      matchUpdateTypes: ["patch", "digest"],
      ignoreTests: false
    },
    {
      description: "Auto-merge OCI Charts",
      matchDatasources: ["docker"],
      automerge: true,
      automergeType: "pr",
      matchUpdateTypes: ["minor", "patch"],
      matchPackageNames: ["/victoria-metrics-k8s-stack/", "/grafana/"],
      ignoreTests: false
    },
    {
      description: "Auto-merge app-template chart updates",
      matchDatasources: ["helm"],
      matchPackageNames: ["/app-template/"],
      automerge: true,
      automergeType: "pr",
      matchUpdateTypes: ["minor", "patch"],
      ignoreTests: false
    },
    {
      description: "Auto-merge trusted GitHub Actions",
      matchManagers: ["github-actions"],
      matchPackageNames: [
        "/^actions\\//",
        "/^renovatebot\\//",
        "/^tj-actions\\//",
        "/^peter-evans\\//",
        "/^mshick\\//",
        "/^docker:\\//"
      ],
      automerge: true,
      automergeType: "branch",
      matchUpdateTypes: ["minor", "patch", "digest"],
      minimumReleaseAge: "1 minute",
      ignoreTests: true
    }
  ]
}
