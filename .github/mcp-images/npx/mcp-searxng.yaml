# yaml-language-server: $schema=https://json-schema.org/draft/2020-12/schema
# Temporary local build until dockyard PR merges:
# https://github.com/stacklok/dockyard/pull/362
---
metadata:
  name: mcp-searxng
  description: MCP server for SearXNG search engine integration
  upstream: https://github.com/stacklok/dockyard/pull/362

spec:
  # renovate: datasource=npm depName=mcp-searxng
  package: mcp-searxng
  version: 0.8.0
