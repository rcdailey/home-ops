[project]
name = "hops"
version = "0.1.0"
description = "LLM-optimized cluster operations CLI"
requires-python = ">=3.13"
dependencies = ["click>=8.1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["hops"]
